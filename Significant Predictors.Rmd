---
title: "Significant Predictors"
author: "Cam Smithers"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Testing for Significant Predictors

```{r}
outcome_var <- "team_upset"

#Choosing predictors for outcome
outcome_predictors <- setdiff(names(all_team_data), outcome_var)



#Creating the structure the models will follow
outcome_var_models <- lapply(outcome_predictors, function(significant_predictor) {
    glm(as.formula(paste(outcome_var, "~", significant_predictor)), 
        data = all_team_data, family = binomial)
})

#Running models and summary
lapply(outcome_var_models, summary)
```

